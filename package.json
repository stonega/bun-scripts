{
  "name": "bun-hono-template",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "echo $GOOGLE_CREDENTIALS > google.credentail.json && bun run src/index.ts",
    "fmt": "bunx @biomejs/biome format --write */**",
    "lint": "bunx @biomejs/biome lint ./src"
  },
  "dependencies": {
    "@biomejs/biome": "^2.0.0",
    "hono": "^4.4.6"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
